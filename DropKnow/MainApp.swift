import SwiftUI

struct DropKnowLegacyEntryPreview: View {
    var body: some View {
        Text("Legacy entry placeholder")
            .frame(minWidth: 320, minHeight: 200)
            .padding()
    }
}
