# 落知（DropKnow）PRD v1

> 产品代号：落知（DropKnow）  
> Slogan：下载即理解，重点即提醒  
> 文档版本：v1.0  
> 更新时间：2026-04-18

---

## 1. 产品概述

### 1.1 产品定义
落知是一款面向大学生与研究生的 macOS 原生菜单栏应用。它持续监听用户授权目录中的新下载文件，在文件落地后自动完成：

- 文件类型识别
- 内容解析与切片
- 重点摘要提炼
- 事件候选抽取（考试、DDL、活动、缴费、课程安排等）
- 应用内轻提示
- 高级语义搜索与日常问答

其核心目标不是做“另一个聊天机器人”，而是做一层位于“文件下载之后、用户阅读之前”的理解层。

### 1.2 核心价值主张
对于学生用户，文件下载后最痛的不是“找不到文件”，而是：

- 文件太多，不想逐个打开看
- 通知、考试安排、作业要求混在一起
- 很多内容只需要抓重点，不需要读全文
- 明明涉及日程，但不会及时进日历
- 后面再想回忆“那个通知说了什么”时，无法按语义快速找回

落知要解决的是：

> 文件刚下载完成，用户就能立刻知道：这是什么、要做什么、什么时候截止、要不要加日历、以后怎么问回来。

---

## 2. 目标用户

### 2.1 核心用户
- 大学生
- 研究生

### 2.2 高频使用场景
- QQ / 微信 / 浏览器下载课程通知、考试安排、报名通知、实验要求
- 老师发 PDF / Word 通知，用户不想逐个打开阅读
- 需要快速确认某份文件里的关键日期、地点、要求
- 想问“上周下载的那个补考通知讲了什么”

### 2.3 高频文件类型
V1 重点支持：
- PDF
- DOCX
- TXT / Markdown（低成本顺带支持）

V1 暂不支持：
- PPT
- Excel
- 图片 OCR

说明：PPT 对课程作业场景有价值，但不属于首版必须能力，后续列入 V1.5/V2。

---

## 3. 产品目标与非目标

### 3.1 V1 产品目标
1. 监听授权目录中的新文件并自动入库
2. 对新文件生成“通知型摘要”
3. 对高置信度时间类信息生成“日历候选”
4. 提供应用内可展开提示卡片，而不是依赖系统通知中心
5. 提供高级语义搜索与限定次数的日常问答
6. 建立免费版 / 订阅版的基础能力边界

### 3.2 非目标（V1 不做）
- 不做普通文件搜索替代 Spotlight
- 不做全盘扫描
- 不做 Excel/PPT/图片 OCR
- 不做自动静默写入日历
- 不做用户自填 API Key 的开放式 provider 管理
- 不做多平台（Windows / iPhone）同步
- 不做重型“工作台式主窗口”产品形态

---

## 4. 版本定位与商业模式

### 4.1 商业模式
采用 **Freemium（免费基础版 + 高级订阅）**。

### 4.2 免费版能力边界（建议）
- 每日自动解析：5 次
- 每日对话问答：5 次
- 高级搜索：限制次数（如每日 5 次）
- 不支持日历能力
- 只能监听 1 个目录：
  - 默认 Downloads；或
  - 1 个自定义目录（二选一）

### 4.3 订阅版能力方向（建议）
- 更高解析额度
- 开启日历候选与加入日历功能
- 默认 Downloads + 自定义目录监听
- 更高高级搜索 / 问答额度
- 后续开放更多文件类型与全局弹窗能力

### 4.4 模型策略
用户不需要配置 API Key。模型调用由应用方统一提供。

V1 仅支持少量内部 provider：
- Qwen
- Zhipu

用户侧只看到：
- 功能结果
- 套餐额度
- 隐私授权提示

而不会暴露底层模型配置复杂度。

---

## 5. 核心用户流程

### 5.1 首次使用流程
1. 用户安装应用
2. 引导授权监听目录
   - 推荐默认 `~/Downloads`
   - 可选择 1 个自定义目录
3. 首次导入最近 7 天文件
4. 静默建索引，不逐条发送通知
5. 应用内显示“已导入最近文件”摘要

### 5.2 后续新文件流程
1. 用户下载新文件
2. 应用监听到新文件落地
3. 等待文件写入稳定
4. 解析文本并判断是否敏感
5. 若敏感，先询问是否解析
6. 若允许，生成：
   - 文件摘要
   - 重点
   - 事件候选
   - 索引数据
7. 右上角应用内弹出轻提示卡片
   - 普通文件：简洁卡片
   - 事件文件：完整卡片 + 操作按钮

### 5.3 后续查询流程
1. 用户从菜单栏打开应用
2. 输入语义化问题，例如：
   - “高数考试在哪天”
   - “上周那个报名通知讲了什么”
3. 系统检索相关文件片段
4. 返回摘要答案 + 关联文件

---

## 6. 功能需求

## 6.1 目录监听

### 需求说明
V1 支持：
- 默认推荐 `~/Downloads`
- 1 个用户自定义目录

### 规则
- 仅监听用户显式授权目录
- 首次仅回扫最近 7 天文件
- 新文件监听为持续行为
- 下载中的临时文件不触发解析
- 文件尺寸稳定后再进入解析队列

### 优先级
P0

---

## 6.2 文件解析

### 支持格式
- PDF
- DOCX
- TXT / MD

### 解析目标
- 提取纯文本
- 按文档结构切片
- 生成基础元信息：
  - 文件名
  - 路径
  - 来源目录
  - 大小
  - 创建/修改时间
  - 文本长度

### 失败策略
- 若解析失败，显示“解析失败”状态
- 允许用户手动重新解析

### 优先级
P0

---

## 6.3 敏感文件识别与门禁

### 需求说明
对于可能包含隐私信息的文件，不直接解析全文，而是先征求用户确认。

### 判断依据
采用轻量规则结合标题与前几行文本判断，例如：
- 成绩单 / 成绩查询
- 身份证 / 护照 / 简历 / 个人信息表
- 银行 / 缴费 / 发票 / 体检 / 医疗
- 高密度手机号、学号、身份证号

### 用户选项（全部提供）
- 本次解析
- 永不解析此文件
- 对此类文件始终询问
- 信任该目录，后续默认解析

### 优先级
P0

---

## 6.4 通知型摘要

### 摘要结构
每个成功解析文件生成固定结构摘要：
- 这是什么文件
- 你要做什么
- 关键时间 / 地点 / 对象
- 3 条重点
- 是否存在日历候选

### 示例模板
- 文件类型：考试安排 / 课程通知 / 作业说明 / 活动通知
- 行动建议：现在阅读 / 记下日期 / 关注截止 / 可忽略
- 重点条目：3 条以内，强压缩表达

### 摘要策略
摘要优先服务“理解和行动”，而不是泛泛地概括全文。

### 优先级
P0

---

## 6.5 事件候选抽取

### 支持事件类型
- exam（考试）
- assignment_deadline（作业截止）
- registration_deadline（报名截止）
- interview（面试/宣讲）
- class_schedule（课程安排）
- meeting（会议通知）
- payment_deadline（缴费截止）
- campus_activity（活动通知）

### 抽取字段
- 事件标题
- 开始时间
- 结束时间（可空）
- 地点（可空）
- 备注
- 证据片段
- 置信度

### 展示规则
- 仅高置信度事件展示“加入日历”按钮
- 中低置信度仅提示“可能涉及时间安排”

### 优先级
P0（订阅版）

---

## 6.6 应用内提示卡片

### 交互风格
不依赖系统通知中心，采用应用内轻提示卡片，位置参考右上角浮层。

### 卡片分类
#### 普通文件
展示：
- 文件名
- 一句话总结
- 展开按钮

#### 事件文件
展示：
- 文件名
- 一句话总结
- 关键时间
- 关键地点（若有）
- 操作按钮：
  - 查看重点
  - 加入日历
  - 忽略
  - 稍后提醒（可选）

### 优先级
P0

---

## 6.7 日历能力

### V1 范围
- 仅订阅版开放
- 不自动写入
- 用户点击按钮后再进入日历写入

### 写入字段
尽可能完整：
- 标题
- 时间
- 地点
- 原文件引用
- 摘要备注
- 提前提醒时间（后续可配置）

### 已处理状态
按推荐方案实现：
- 文件卡片显示“已加入日历”
- 支持从应用内打开该事件（若实现成本可控）
- 不做事件自动更新同步

### 优先级
P1（订阅版）

---

## 6.8 高级搜索与日常问答

### 定位
不做普通搜索替代 Spotlight。仅做基于解析内容的高级语义搜索与问答。

### 支持查询类型
- 找某个通知的核心内容
- 找某个考试 / DDL / 活动的时间地点
- 找某份已下载文件的关键要求

### 结果形式
- 直接答案
- 关联文件
- 证据摘要

### V1 范围
- 菜单栏内入口
- 基础语义问答
- 限定额度

### V1 不做
- 双击 Control 全局半透明弹窗
- 长上下文多轮复杂 Agent

### 优先级
P1

---

## 6.9 文件优先级判断

按推荐方案，系统自动给文件打优先级：
- 高优先级：考试、DDL、缴费、强行动项
- 普通：课程资料、阅读材料
- 可忽略：参考资料、非紧急附件

该优先级将影响：
- 是否弹出提示
- 卡片样式
- 重要提醒页面排序

### 优先级
P1

---

## 6.10 手动重新解析

### 用户能力
在文件详情页支持：
- 重新摘要
- 重新抽取事件
- 重新索引

### 使用场景
- 模型提取出错
- 文件内容被更新
- 用户切换了模型策略

### 优先级
P1

---

## 7. 页面与信息架构

## 7.1 菜单栏菜单
- 最近下载
- 重要提醒
- 高级搜索
- 设置
- 退出

## 7.2 最近下载页
每条卡片展示：
- 文件名
- 下载时间
- 文件类型
- 一句话说明
- 是否已索引
- 是否已加入日历

## 7.3 重要提醒页
仅展示：
- 高优先级文件
- 有事件候选文件
- 可一键加入日历
- 可标记已处理

## 7.4 高级搜索页
输入自然语言问题，返回：
- 答案摘要
- 命中文件
- 命中证据片段

## 7.5 文件详情页
默认行为按推荐方案：
- 搜索结果先进入应用内详情
- 详情页可“打开原文件”

详情页内容：
- 基本信息
- 通知型摘要
- 关键重点
- 事件候选
- 原文片段
- 操作按钮

## 7.6 设置页
### 基础设置
- 开机启动
- 首次仅导入最近 7 天
- 自动解析新文件

### 目录设置
- 默认下载目录
- 自定义目录

### 隐私设置
- 敏感文件始终询问
- 信任目录
- 黑名单文件

### 套餐与额度
- 今日解析剩余次数
- 今日问答剩余次数
- 升级入口

---

## 8. 用户体验规则

## 8.1 首次接入规则
- 只回扫最近 7 天
- 不逐条弹提示
- 应用内显示汇总结果

## 8.2 后续新文件提示规则
- 高优先级：弹完整卡片
- 普通：弹简洁卡片
- 可忽略：默认静默入库

## 8.3 日历规则
- 绝不自动静默入历
- 仅用户确认后写入
- 写入后更新卡片状态

## 8.4 隐私规则
- 敏感文件默认先询问
- 允许用户建立目录信任策略

---

## 9. 数据定义（产品层）

## 9.1 文件实体
- file_id
- file_name
- file_path
- source_directory
- file_type
- imported_at
- parsed_status
- sensitivity_status
- priority_level
- summary_status
- event_status

## 9.2 摘要实体
- summary_id
- file_id
- file_type_label
- action_hint
- key_time
- key_location
- key_points[]
- one_line_summary

## 9.3 事件实体
- event_candidate_id
- file_id
- event_type
- title
- start_time
- end_time
- location
- note
- confidence
- calendar_status

## 9.4 额度实体
- daily_parse_count
- daily_chat_count
- daily_advanced_search_count
- current_plan

---

## 10. 成功指标（V1）

### 产品指标
- 新文件解析成功率
- 摘要生成成功率
- 事件候选点击率
- 日历写入转化率（订阅版）
- 高级搜索使用率
- 7 日留存

### 体验指标
- 从文件落地到摘要可见的平均耗时
- 高优先级文件误报率
- 敏感文件误解析投诉率

### 商业指标
- 免费用户到订阅页点击率
- 订阅转化率
- 配额耗尽后的次日回访率

---

## 11. 风险与约束

### 11.1 内容风险
- PDF / DOCX 解析质量不稳定
- 同类通知格式差异大
- 事件抽取易误判

### 11.2 隐私风险
- 用户对“上传解析”敏感
- 敏感文件识别可能漏判

### 11.3 产品风险
- 若卡片过多，会造成打扰
- 若摘要过泛，会失去核心价值
- 若功能边界过大，MVP 容易延期

### 11.4 版本控制原则
V1 只做：
- 下载即理解
- 高置信度事件提醒
- 限定额度的高级搜索

其余全部后移。

---

## 12. V1 验收标准

满足以下条件即可视为 V1 可发布测试：

1. 用户能成功授权并监听默认目录/自定义目录
2. 首次只导入最近 7 天文件，且无通知轰炸
3. PDF / DOCX 文件可稳定解析
4. 新文件可在合理时间内得到摘要
5. 高优先级事件文件可显示加入日历入口
6. 敏感文件会先询问再解析
7. 用户可用自然语言询问已下载文件内容
8. 免费版 / 订阅版能力差异可被正确限制

---

## 13. 命名建议

### 主推荐名
# 落知（DropKnow）

含义：
- “落”对应文件落地、下载完成
- “知”对应理解、提醒、找回
- “落知”四个字概念浓缩为：**文件一落地，你立刻知道重点**

### 备选名
- 文件雷达
- 即知
- 校知

建议正式对外采用：
- 中文名：落知
- 英文名：DropKnow
- 宣传语：下载即理解，重点即提醒

