# 落知（DropKnow）V1 性能预算与优化策略

> 目标：让“下载即理解”在真实 Mac 使用场景下体感顺滑，而不是只在功能上能跑通

---

# 1. 文档目标

本文件用于定义：

1. V1 各模块目标耗时
2. 主线程禁区
3. 队列、并发与缓存策略
4. 首次导入限流策略
5. 搜索与问答的响应目标
6. 指标与埋点口径

---

# 2. 性能原则

1. **UI 永远不等待解析与模型返回**
2. **监听发现 != 立即同步处理**
3. **首次导入与实时监听分队列**
4. **摘要与事件抽取分阶段，不一股脑并发**
5. **能缓存的结果必须缓存**
6. **多卡片同时到来必须合并/排队**
7. **性能目标先围绕体感，而不是理论吞吐**

---

# 3. 关键链路与目标耗时

## 3.1 新文件实时链路

```text
文件落地
-> 稳定检测
-> 入队
-> 文本提取
-> 敏感门禁
-> 摘要
-> 事件抽取
-> 落库
-> 卡片展示
```

### 目标耗时（普通 PDF / DOCX）

| 阶段 | 目标耗时 | 备注 |
|---|---:|---|
| 监听命中 | < 200ms | 只负责发现事件 |
| 稳定检测 | 1s ~ 3s | 取决于下载完成 |
| 入队与去重 | < 100ms | 本地元数据与 hash |
| 文本提取 | < 800ms | 普通 1~10 页文档 |
| 门禁判断 | < 100ms | 纯本地规则 |
| 摘要请求 | 1.5s ~ 4s | 主要受网络/provider 影响 |
| 事件抽取 | 0.8s ~ 2.5s | 可串在摘要之后 |
| 落库 | < 50ms | 批量写事务 |
| 卡片出现 | 文件完成后 100ms 内 | UI 更新迅速 |

### 端到端体感目标
- **普通文件**：3s ~ 8s 内可见最终摘要
- **事件文件**：4s ~ 10s 内可见完整卡片

## 3.2 首次导入链路

首次导入最近 7 天文件的目标不是“瞬间完成”，而是：

- 不打断用户
- 不拖慢系统
- 可渐进完成

### 目标
- 首屏汇总状态应在 1s 内出现
- 后台持续接入文件
- 单批并发不超过 2~3 个文件
- 不逐条弹卡片

## 3.3 搜索 / 问答链路

### 目标耗时

| 场景 | 目标 |
|---|---:|
| 本地事件检索 | < 150ms |
| 本地摘要/FTS 检索 | < 250ms |
| 证据拼装 | < 100ms |
| 需要模型生成答案时 | 1s ~ 3s |
| 完整问答体感 | < 4s |

---

# 4. 主线程禁区

以下任务禁止运行在主线程：

- 目录扫描
- hash 计算
- 文件解析
- 文本切片
- SQLite 大事务写入
- provider 网络请求
- JSON 修复
- FTS 批量写入
- 搜索重排序

主线程只做：

- 场景路由
- 卡片展示
- 列表刷新
- 简单状态绑定
- 用户动作响应

---

# 5. 队列设计

## 5.1 推荐队列拆分

- `watchQueue`
  - 负责监听事件收集
- `importQueue`
  - 负责文件稳定检测、去重、元数据处理
- `parseQueue`
  - 负责文本提取
- `intelligenceQueue`
  - 负责摘要、事件抽取、可选 JSON 修复
- `indexQueue`
  - 负责写库、切片、FTS
- `uiQueue/MainActor`
  - 负责状态更新与显示

## 5.2 并发建议

| 队列 | 并发建议 | 原因 |
|---|---:|---|
| watchQueue | 1 | 保持事件顺序简单 |
| importQueue | 2 | 轻量任务，可小并发 |
| parseQueue | 2 | 避免 CPU / IO 抖动 |
| intelligenceQueue | 1~2 | 避免模型请求暴涨 |
| indexQueue | 1 | 降低 DB 锁竞争 |

---

# 6. 首次导入策略

## 6.1 规则

- 只扫最近 7 天
- 先拉元数据，再决定是否解析
- 首次导入使用低优先级队列
- 不逐条弹提醒
- 先把“最近文件列表”填起来，再慢慢补摘要

## 6.2 建议顺序

1. 扫描目录得到文件列表
2. 先写 `documents` 基础元数据
3. UI 立刻能显示“已接入最近文件”
4. 后台逐个解析与补摘要
5. 重要文件补进“重要提醒”

---

# 7. 缓存策略

## 7.1 必须缓存的结果

- 文件 hash
- 解析后的纯文本
- 摘要 JSON
- 事件候选 JSON
- 重要度评分
- FTS 文本
- 最后一次错误码

## 7.2 不应重复做的工作

同一版本的文件内容不应重复：
- 重新摘要
- 重新事件抽取
- 重新切片
- 重新建立索引

只有以下情况才重算：
- 文件 hash 变化
- 用户手动重新解析
- prompt / schema 版本变更
- provider 切换策略明确触发重算

---

# 8. 卡片与 UI 性能策略

## 8.1 卡片展示策略

- 普通文件：自动消失、单条展示
- 事件文件：完整卡片、可操作
- 同时到来多个文件：
  - 合并为“3 个新文件已处理”
  - 或进入队列逐个展示
- 最大同时显示卡片数：1

## 8.2 列表性能

- 最近文件列表默认分页/懒加载
- Detail Window 左栏只展示必要字段
- 摘要与证据片段分块渲染
- 不在滚动时做重计算

---

# 9. 搜索性能策略

## 9.1 检索顺序

1. `document_events`
2. `document_summaries_fts`
3. `document_chunks_fts`
4. 必要时才走语义召回

## 9.2 原因

你的产品不做 Finder 替代，搜索主要围绕：
- 事件
- 摘要
- 重点
- 证据

所以不要一上来全量扫 chunks 或跑重型向量搜索。

---

# 10. 数据库优化建议

## 10.1 必建索引

- `documents(file_hash)`
- `documents(imported_at)`
- `documents(ingest_status)`
- `documents(event_status)`
- `documents(privacy_status)`
- `document_events(document_id)`
- `document_events(start_time)`
- `document_events(decision_status)`
- `parse_jobs(document_id, stage)`

## 10.2 写入策略

- 批量事务写入
- 文本表与主表分离
- chunk 写入与 summary/event 写入解耦
- 搜索索引异步建立

---

# 11. 监控指标表

## 11.1 实时链路指标

| 指标名 | 含义 |
|---|---|
| watch_detect_to_queue_ms | 监听到入队耗时 |
| queue_to_parse_start_ms | 排队等待耗时 |
| parse_duration_ms | 解析耗时 |
| gate_duration_ms | 门禁耗时 |
| summary_duration_ms | 摘要耗时 |
| event_extract_duration_ms | 事件抽取耗时 |
| db_write_duration_ms | 落库耗时 |
| file_detect_to_card_ms | 端到端卡片耗时 |

## 11.2 首次导入指标

| 指标名 | 含义 |
|---|---|
| initial_scan_file_count | 首次导入文件数 |
| initial_scan_total_duration_ms | 首次导入总耗时 |
| initial_scan_parse_success_rate | 首次导入解析成功率 |
| initial_scan_summary_success_rate | 首次导入摘要成功率 |

## 11.3 搜索指标

| 指标名 | 含义 |
|---|---|
| search_local_hit_ms | 本地命中耗时 |
| search_answer_total_ms | 问答总耗时 |
| search_no_result_rate | 无结果率 |
| search_quota_block_rate | 额度阻断率 |

---

# 12. 体验红线

以下任一长期超标，都应视为 P1 性能问题：

- 普通新文件 10s 后仍无摘要
- 首次导入导致 UI 卡顿明显
- 同一文件重复触发处理
- 搜索超过 5s 才出结果
- 切换 Quick Panel 明显卡顿
- 列表滚动掉帧
- 免费用户额度耗尽后无清晰反馈

---

# 13. 优化优先级建议

## 第一优先
- 去抖与去重
- 异步队列
- 缓存摘要/事件结果
- 首次导入低优先级化

## 第二优先
- FTS 索引与查询优化
- 详情页分块渲染
- 事件优先检索

## 第三优先
- 语义召回优化
- provider 并发策略优化
- 更精细的预取与懒加载

---

# 14. 最终建议

在 V1 阶段，不要为了“未来也许会很大”而提前上过重架构。

只要你把：

- 队列拆对
- 主线程守住
- 首次导入限流
- 结果缓存好
- 搜索先走事件/摘要

这套选型就足够顺滑，也足够支撑早期商业化测试。
